const getters = {

};

const actions = {
    login({commit}, payload) {
        commit('login', payload);
    }
};

const mutations = {
    login(state, payload) {

    }
};


export default {
    getters,
    mutations,
    actions,
}